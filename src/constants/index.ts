import FUIC from '@constants/FluentComponentsNames'

const CURRENCIES = {
  USD: 'USD',
  CRC: 'CRC'
}

const CONSTANTS = {
  CURRENCIES,
  /** Map of Fluent UI web components' names */
  FUIC
}

export default CONSTANTS